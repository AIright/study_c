#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <algorithm>

using namespace std;


int main() {
    int tries;
    string param1;
    vector<string> buses;
    map<string, vector<string>> routes;
    cin >> tries;
    for (int i=0; i<tries; i++) {
    }
    return 0;
};